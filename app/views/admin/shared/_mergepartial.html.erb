<h3>Merge Articles<h3>
  <%= form_tag(form_action, :id => "mergepartial_form") do %>
<%= label_tag(:article_id, "Article ID") %>
<%= text_field(:article_id) %>
    <%= submit_tag("Merge") %>
    <% end %>
